General
- synchronized color schemes
- gcroots
- glab, gh
- fix git aliases
- look into workmux
- fix bash and zsh configs so that there is feature parity. Bonus: respect XDG
- give bash a distinguished look
- create standalone .bashrc, .tmux.conf, and .vimrc for copying onto servers
- try new niri and aerospace keybindings to have better window movement behavior
- nixpkgs multiverse
- configure yazi: keybindings closer to nvim/oil
- allow shell detect nerd fonts to change prompt
- fix cursor in fish and nvim when using tmux and not from rootshell and not
- fix clipboard going through all of rootshell, ghostty, tmux, ssh, nvim

Secrets
- bootstrap procedure: create ssh, git auth, tailscale, onepassword, etc.

Neovim
- neovim cold startup is bad
- convert nvim config to nix (try nvf)
- normal mode cursor in rootshell is line
- figure out actually good diff settings
- try 0.13

Tmux
- how can I name/preview sessions/windows better so I know what is happening
- config for mobile UI
- change color depending on local or remote tmux server
- configure remote tmux sessions to connect to local clipboard
- in bash shell, prefix+] adds the ~[200 stuff
- holding Ctrl-d to scroll down will eventually hit the bottom, exit select, and close every fucking shell

NixOS
- automatic security updates
- running nixos-rebuild over ssh breaks something
- checkout nixos-hardware for custom hardware patches

Theta
- explore boot-to-network-ready optimizations (current baseline: wired network online around 31 seconds after power-on; Tailscale around 33 seconds):
  - reduce `boot.loader.timeout` from 5 to 1 second
  - add `e1000e` to `boot.initrd.availableKernelModules` so Ethernet link negotiation overlaps initrd startup
  - test BIOS Fast Boot and disable unused PXE/network/USB boot paths
  - consider static IPv4 only after reserving the address on the router
  - keep `NetworkManager-wait-online` enabled and measure actual carrier, DHCP, network-online, and Tailscale readiness after each change
- finish Hermes Agent setup after deploying its container:
  - rebuild theta so the SOPS template and container are restarted
  - run `sudo podman exec -it --user hermes hermes-agent hermes auth add openai-codex` and complete the ChatGPT device-code login
  - if Tailscale Serve reports that HTTPS is disabled, follow its admin-console link once to enable tailnet certificates, then restart `hermes-agent-tailscale-serve.service`
  - verify Hermes WebUI at https://theta.bongo-sidemirror.ts.net and use that URL plus the dashboard password in Hermex
  - verify the native dashboard at https://theta.bongo-sidemirror.ts.net:9443 from a tailnet device
- finish Preservation rollout:
  - create and seed /persist, then enable it in a boot generation
  - make / and /var ephemeral while keeping /home persistent initially
  - remove one preserved path at a time, quarantine old state, and validate across two reboots
  - add a VM reboot test for preserved and ephemeral state
- services: homepage, arcane, freshrss, SOTD,

Alpha:
- pocketid
